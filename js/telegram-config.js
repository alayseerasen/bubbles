/* BUBBLES — Telegram bot config
   ------------------------------------------------------------
   Замени на юзернейм бота, который ты создашь через @BotFather (см.
   supabase/functions/telegram-bot/README.md, шаг 2) — БЕЗ символа "@".
   Это единственное, что нужно поменять во фронтенде: сам бот и вся
   логика привязки живут на сервере (Edge Function + Supabase), здесь
   только имя, которое показывается человеку в интерфейсе Bubbles.
*/
window.BUBBLES_TELEGRAM_BOT_USERNAME = "BubblesNotificationsBot";
